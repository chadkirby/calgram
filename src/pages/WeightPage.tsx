import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function WeightPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Record Weight</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Weight tracking functionality will be implemented here.</p>
      </CardContent>
    </Card>
  );
}